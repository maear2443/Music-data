# 음악 메타데이터 관리 시스템 - 필수 라이브러리

# 웹 UI
streamlit>=1.31.0

# 음악 분석 (버전 범위 유연하게)
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0

# 음악 처리
pydub>=0.25.1

# 엑셀 처리
openpyxl>=3.1.0
pandas>=2.0.0

# 과학 계산 (미리 컴파일된 버전 사용)
numpy>=1.24.0
scipy>=1.10.0

# 기타 의존성
setuptools>=65.5.1
